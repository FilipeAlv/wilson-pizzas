<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.wilsonpizzas">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Hitorico"></activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.Splash"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ActivityMeusPedidos"
            android:label="@string/title_activity_meus_pedidos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.Login" />
        <activity
            android:name=".activity.ActivityCadastro"
            android:label="Casdastrar Usuario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.ConfirmarPedido" />
        <activity
            android:name=".activity.ActivityEqueceuSenha"
            android:label="@string/title_activity_esqueci__minha__senha"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.AdicionarSaborPizza" />
        <activity android:name=".activity.NovaSenha" />
    </application>

</manifest>